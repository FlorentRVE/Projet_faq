{% extends 'base.html.twig' %}

{% block title %}Modifier une question{% endblock %}

{% block body %}
    <h1 class="text-center text-3xl uppercase text-slate-100 font-bold bg-slate-800 py-5 shadow-xl">Modifier une demande</h1>

    {# ======== Formulaire de modification ========= #}
    {{ include('question/_form.html.twig', {'button_label': 'Modifier'}) }}

    
    {# ========= Boutons d'actions ======== #}
    <div class="flex justify-between w-4/5 mx-auto">
        <a href="{{ path('app_question_index') }}" class="bg-sky-800 text-slate-50 rounded-2xl text-center px-5 py-3 hover:brightness-125 shadow-lg font-semibold">Retour à la liste</a>

        {{ include('question/_delete_form.html.twig') }}
    </div>
{% endblock %}
