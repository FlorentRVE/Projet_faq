{% extends 'base.html.twig' %}

{% block title %}Modifier une demande{% endblock %}

{% block body %}
    <h1 class="text-center text-3xl uppercase text-slate-100 font-bold bg-blue-900 py-5 shadow-xl">Modifier une demande</h1>

    {# ======== Formulaire de modification ========= #}
    {{ include('demande/_form.html.twig', {'button_label': 'Modifier'}) }}

    
    {# ========= Boutons d'actions ======== #}
    <div class="flex justify-between w-4/5 mx-auto">
        <a href="{{ path('app_demande_index') }}" class="bg-orange-500 rounded-2xl text-center px-5 py-3 hover:brightness-125 shadow-lg font-semibold">Retour à la liste</a>

        {{ include('demande/_delete_form.html.twig') }}
    </div>
{% endblock %}
