{% extends 'base.html.twig' %}

{% block title %}Edit Saisi{% endblock %}

{% block body %}
    <h1 class="text-center text-3xl uppercase text-slate-100 font-bold bg-slate-800 py-5 shadow-xl">Modifier saisi</h1>

    {{ include('saisi/_form.html.twig', {'button_label': 'Modifier'}) }}

        <div class="flex justify-between w-4/5 mx-auto">
        <a href="{{ path('app_saisi_index') }}"  class="bg-sky-800 text-slate-50 rounded-2xl text-center px-5 py-3 hover:brightness-125 shadow-lg font-semibold">Retour à la liste</a>
    </div>

    {{ include('saisi/_delete_form.html.twig') }}
{% endblock %}
