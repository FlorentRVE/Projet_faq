{% extends 'base.html.twig' %}

{% block title %}Modifier Departement{% endblock %}

{% block body %}
    <h1 class="text-center text-3xl uppercase text-slate-100 font-bold bg-slate-800 py-5 shadow-xl">Modifier un département</h1>

    {{ include('departement/_form.html.twig', {'button_label': 'Modifier'}) }}

    {# ========= Boutons d'actions ======== #}
    <div class="flex justify-between w-4/5 mx-auto">
        <a href="{{ path('app_departement_index') }}" class="bg-blue-800 text-slate-50 rounded-2xl text-center px-5 py-3 hover:brightness-125 shadow-lg font-semibold">Retour à la liste</a>

        {{ include('departement/_delete_form.html.twig') }}
    </div>
{% endblock %}
